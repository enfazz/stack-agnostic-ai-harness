{
  "name": "fixture-node-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "vite build"
  }
}
